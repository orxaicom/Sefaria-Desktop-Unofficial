{
  "name": "sefaria-electron",
  "version": "1.0.0",
  "description": "Sefaria Electron App",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron --no-sandbox .",
    "build": "electron-builder --no-sandbox",
    "build:appimage": "electron-builder --no-sandbox --linux AppImage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^32.1.2"
  }
}
